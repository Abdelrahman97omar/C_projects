/*this code is to remove the prime numbers, it takes two parameters
the number to remove prims form(num_to_edit
and the number of digits of the number (*number_of_digits)*/
#include<stdio.h>

int remove_prime(int num_to_edit, int *number_of_digits);
{
    for (int i = 10; i < number_of_digits; i=i*10)
    {
  
    }
    
}